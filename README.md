# Telegram Multi Session Joiner Bot

Bot to manage Telethon StringSessions, extract Telegram links from private channels, distribute 1000 links per session without duplicates, and auto join groups/channels.

## Setup (Local)
1. Copy `.env.example` to `.env`
2. Fill env values
3. Install:
   ```bash
   pip install -r requirements.txt
